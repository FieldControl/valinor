{
  "name": "valinor-monorepo",
  "private": true,
  "scripts": {
    "start:backend": "cd app-backend && npm run start:dev",
    "start:frontend": "cd client    && npm run start"
  },
  "devDependencies": {
    "concurrently": "^7.0.0"
  },
  "dependencies": {
    "@angular/cdk": "^20.0.3"
  }
}
