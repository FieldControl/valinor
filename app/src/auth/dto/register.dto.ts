export class RegisterDto { // Os arquivos .dto são usados para definir os objetos de transferência de dados (DTOs) que serão usados nas requisições e respostas da API.
  email: string;
  password: string;
  firstName: string;
  lastName: string;
}
