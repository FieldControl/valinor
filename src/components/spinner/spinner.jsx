import React from 'react';
import { SpinnerContainer, SpinnerLoader } from './styles';

const Spinner = () => {
  return (
    <SpinnerContainer>
      <SpinnerLoader />
    </SpinnerContainer>
  );
};

export default Spinner;
