import { StarCountPipe } from './star-count.pipe';

describe('StarCountPipe', () => {
  it('create an instance', () => {
    const pipe = new StarCountPipe();
    expect(pipe).toBeTruthy();
  });
});
