import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `<app-core-root></app-core-root>`
})

/**
 * IMPORTANTE! Não é necessário configurar nada no app.component, para deixa-lo limpo,
 * qualquer configuração que você irá fazer aqui pode ser feita no core.component!
 * A arquitetura do app foi preparada para isso!
 */
export class AppComponent {}
